import { c as createComponent } from './astro-component_BX-qrZ11.mjs';
import 'piccolore';
import { r as renderTemplate, m as maybeRenderHead, b as renderComponent, c as Fragment } from './server_DjVGnHP9.mjs';
import { $ as $$OperatorShell } from './OperatorShell_B4gj5AHW.mjs';
import { $ as $$CorpusResolverPanel } from './CorpusResolverPanel_DkWs4U3g.mjs';
import 'clsx';

var __freeze$1 = Object.freeze;
var __defProp$1 = Object.defineProperty;
var __template$1 = (cooked, raw) => __freeze$1(__defProp$1(cooked, "raw", { value: __freeze$1(raw || cooked.slice()) }));
var _a$1;
const $$RunSummaryCard = createComponent(($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$props, $$slots);
  Astro2.self = $$RunSummaryCard;
  const { title = "Run Summary", initialRun = null } = Astro2.props;
  return renderTemplate(_a$1 || (_a$1 = __template$1(["", '<section class="run-summary-card" data-run-summary-card data-astro-cid-oq76engr> <header class="run-summary-card__header" data-astro-cid-oq76engr> <h2 data-astro-cid-oq76engr>', '</h2> <button type="button" id="run-summary-clear" data-astro-cid-oq76engr>Clear</button> </header> <div id="run-summary-body" data-astro-cid-oq76engr> ', ' </div> <script type="module">\n    const root = document.currentScript?.closest("[data-run-summary-card]");\n    const body = root?.querySelector("#run-summary-body");\n    const clearButton = root?.querySelector("#run-summary-clear");\n\n    function renderRun(run) {\n      if (!body) return;\n\n      if (!run) {\n        body.innerHTML = `\n          <p class="run-summary-card__placeholder">\n            No run selected. Inspect or stage a casepack to populate this card.\n          </p>\n        `;\n        return;\n      }\n\n      const invariants = run.invariants ?? null;\n      const stance = run.stance ?? null;\n      const artifacts = run.artifacts ?? [];\n\n      body.innerHTML = `\n        <div class="run-summary-card__content">\n          <p><strong>Run ID:</strong> ${run.runId ?? "—"}</p>\n          <p><strong>Status:</strong> ${run.status ?? "—"}</p>\n          <p><strong>Casepack:</strong> ${run.casepackId ?? "—"}</p>\n          <p><strong>Contract:</strong> ${run.contractId ?? "—"}</p>\n\n          <h3>Invariants</h3>\n          <pre>${JSON.stringify(invariants, null, 2)}</pre>\n\n          <h3>Stance</h3>\n          <pre>${JSON.stringify(stance, null, 2)}</pre>\n\n          <h3>Artifacts</h3>\n          <pre>${JSON.stringify(artifacts, null, 2)}</pre>\n        </div>\n      `;\n    }\n\n    window.addEventListener("operator:run-update", (event) => {\n      renderRun(event.detail);\n    });\n\n    window.addEventListener("operator:run-clear", () => {\n      renderRun(null);\n    });\n\n    clearButton?.addEventListener("click", () => {\n      window.dispatchEvent(new CustomEvent("operator:run-clear"));\n      renderRun(null);\n    });\n  <\/script> </section>'], ["", '<section class="run-summary-card" data-run-summary-card data-astro-cid-oq76engr> <header class="run-summary-card__header" data-astro-cid-oq76engr> <h2 data-astro-cid-oq76engr>', '</h2> <button type="button" id="run-summary-clear" data-astro-cid-oq76engr>Clear</button> </header> <div id="run-summary-body" data-astro-cid-oq76engr> ', ' </div> <script type="module">\n    const root = document.currentScript?.closest("[data-run-summary-card]");\n    const body = root?.querySelector("#run-summary-body");\n    const clearButton = root?.querySelector("#run-summary-clear");\n\n    function renderRun(run) {\n      if (!body) return;\n\n      if (!run) {\n        body.innerHTML = \\`\n          <p class="run-summary-card__placeholder">\n            No run selected. Inspect or stage a casepack to populate this card.\n          </p>\n        \\`;\n        return;\n      }\n\n      const invariants = run.invariants ?? null;\n      const stance = run.stance ?? null;\n      const artifacts = run.artifacts ?? [];\n\n      body.innerHTML = \\`\n        <div class="run-summary-card__content">\n          <p><strong>Run ID:</strong> \\${run.runId ?? "—"}</p>\n          <p><strong>Status:</strong> \\${run.status ?? "—"}</p>\n          <p><strong>Casepack:</strong> \\${run.casepackId ?? "—"}</p>\n          <p><strong>Contract:</strong> \\${run.contractId ?? "—"}</p>\n\n          <h3>Invariants</h3>\n          <pre>\\${JSON.stringify(invariants, null, 2)}</pre>\n\n          <h3>Stance</h3>\n          <pre>\\${JSON.stringify(stance, null, 2)}</pre>\n\n          <h3>Artifacts</h3>\n          <pre>\\${JSON.stringify(artifacts, null, 2)}</pre>\n        </div>\n      \\`;\n    }\n\n    window.addEventListener("operator:run-update", (event) => {\n      renderRun(event.detail);\n    });\n\n    window.addEventListener("operator:run-clear", () => {\n      renderRun(null);\n    });\n\n    clearButton?.addEventListener("click", () => {\n      window.dispatchEvent(new CustomEvent("operator:run-clear"));\n      renderRun(null);\n    });\n  <\/script> </section>'])), maybeRenderHead(), title, initialRun ? renderTemplate`<div class="run-summary-card__content" data-astro-cid-oq76engr> <p data-astro-cid-oq76engr><strong data-astro-cid-oq76engr>Run ID:</strong> ${initialRun.runId ?? "—"}</p> <p data-astro-cid-oq76engr><strong data-astro-cid-oq76engr>Status:</strong> ${initialRun.status ?? "—"}</p> <p data-astro-cid-oq76engr><strong data-astro-cid-oq76engr>Casepack:</strong> ${initialRun.casepackId ?? "—"}</p> <p data-astro-cid-oq76engr><strong data-astro-cid-oq76engr>Contract:</strong> ${initialRun.contractId ?? "—"}</p> <h3 data-astro-cid-oq76engr>Invariants</h3> <pre data-astro-cid-oq76engr>${JSON.stringify(initialRun.invariants ?? null, null, 2)}</pre> <h3 data-astro-cid-oq76engr>Stance</h3> <pre data-astro-cid-oq76engr>${JSON.stringify(initialRun.stance ?? null, null, 2)}</pre> <h3 data-astro-cid-oq76engr>Artifacts</h3> <pre data-astro-cid-oq76engr>${JSON.stringify(initialRun.artifacts ?? [], null, 2)}</pre> </div>` : renderTemplate`<p class="run-summary-card__placeholder" data-astro-cid-oq76engr>
No run selected. Inspect or stage a casepack to populate this card.
</p>`);
}, "/home/runner/work/GENERATIVE-COLLAPSE-DYNAMICS/GENERATIVE-COLLAPSE-DYNAMICS/web/src/components/RunSummaryCard.astro", void 0);

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Casepacks = createComponent(async ($$result, $$props, $$slots) => {
  const bootstrapSession = {
    sessionId: "bootstrap",
    createdAt: (/* @__PURE__ */ new Date()).toISOString(),
    updatedAt: (/* @__PURE__ */ new Date()).toISOString()
  };
  return renderTemplate`${renderComponent($$result, "OperatorShell", $$OperatorShell, { "title": "CAI · Casepacks", "sessionId": bootstrapSession.sessionId, "data-astro-cid-jvymcrff": true }, { "default": async ($$result2) => renderTemplate(_a || (_a = __template(["  ", '<section class="casepacks-page" data-astro-cid-jvymcrff> <article class="operator-main-card" data-astro-cid-jvymcrff> <header class="operator-main-card__header" data-astro-cid-jvymcrff> <h2 data-astro-cid-jvymcrff>Casepacks</h2> <button type="button" id="casepacks-refresh" data-astro-cid-jvymcrff>Refresh</button> </header> <p id="casepacks-status" data-astro-cid-jvymcrff>Loading casepacks…</p> <ul class="casepacks-list" id="casepacks-list" data-astro-cid-jvymcrff> <li class="casepacks-list__placeholder" data-astro-cid-jvymcrff>No casepacks found.</li> </ul> </article> <article class="operator-main-card" data-astro-cid-jvymcrff> <h2 data-astro-cid-jvymcrff>Selected Casepack</h2> <pre id="casepacks-detail-json" data-astro-cid-jvymcrff>No casepack selected.</pre> </article> </section>  <script type="module">\n    import { getCorpusObject, searchCorpus } from "../../lib/operator-api.ts";\n    import { ensureOperatorSession } from "../../lib/operator-session.ts";\n\n		const sessionStatus = document.getElementById("casepacks-session-status");\n		const sessionIdEl = document.getElementById("casepacks-session-id");\n		const contractEl = document.getElementById("casepacks-contract-id");\n		const casepacksStatus = document.getElementById("casepacks-status");\n		const casepacksList = document.getElementById("casepacks-list");\n		const detailJson = document.getElementById("casepacks-detail-json");\n		const refreshButton = document.getElementById("casepacks-refresh");\n\n		let sessionId = "";\n		let activeContractId = "UMA.INTSTACK.v1";\n\n		function setStatus(text) {\n			if (casepacksStatus) casepacksStatus.textContent = text;\n		}\n\n		function renderCasepacks(results) {\n			if (!casepacksList) return;\n\n			if (!results.length) {\n				casepacksList.innerHTML = `<li class="casepacks-list__placeholder">No casepacks found.</li>`;\n				return;\n			}\n\n			casepacksList.innerHTML = results.map((item) => `\n				<li class="casepacks-list__item">\n					<div>\n						<strong>${item.title}</strong>\n						<p>${item.kind}:${item.id}</p>\n						${item.summary ? `<small>${item.summary}</small>` : ""}\n					</div>\n					<div class="casepacks-list__actions">\n						<button type="button" data-inspect-id="${item.id}" data-inspect-title="${item.title}">Inspect</button>\n						<button type="button" data-run-id="${item.id}" data-run-title="${item.title}">Run</button>\n					</div>\n				</li>\n			`).join("");\n		}\n\n		async function inspectCasepack(casepackId, title = casepackId) {\n			try {\n				const response = await getCorpusObject("casepack", casepackId);\n\n				if (detailJson) {\n					detailJson.textContent = JSON.stringify(response.object, null, 2);\n				}\n\n				window.dispatchEvent(\n					new CustomEvent("operator:run-update", {\n						detail: {\n							runId: `staged:${casepackId}`,\n							status: "staged",\n							casepackId,\n							contractId: activeContractId,\n							invariants: null,\n							stance: {\n								verdict: "NON_EVALUABLE",\n								rationale: [\n									"Casepack selected and inspected.",\n									"Authoritative run execution endpoint is not wired in this pass."\n								]\n							},\n							artifacts: [],\n						},\n					}),\n				);\n\n				setStatus(`Inspected casepack ${title}.`);\n			} catch (error) {\n				if (detailJson) {\n					detailJson.textContent =\n						error instanceof Error ? error.message : "Failed to inspect casepack.";\n				}\n				setStatus("Inspection failed.");\n			}\n		}\n\n		async function loadCasepacks() {\n			setStatus("Loading casepacks…");\n\n			try {\n				const response = await searchCorpus({\n					query: "",\n					kinds: ["casepack"],\n					limit: 50,\n				});\n\n				renderCasepacks(response.results);\n				setStatus(`Loaded ${response.results.length} casepack(s).`);\n			} catch (error) {\n				renderCasepacks([]);\n				setStatus(error instanceof Error ? error.message : "Failed to load casepacks.");\n			}\n		}\n\n		async function pollRun(runId) {\n			const maxAttempts = 12;\n			const delayMs = 250;\n\n			for (let attempt = 0; attempt < maxAttempts; attempt += 1) {\n				const run = await getRun(runId);\n\n				window.dispatchEvent(\n					new CustomEvent("operator:run-update", { detail: run }),\n				);\n\n				if (run.status === "completed" || run.status === "failed") {\n					return run;\n				}\n\n				await new Promise((resolve) => setTimeout(resolve, delayMs));\n			}\n\n			return getRun(runId);\n		}\n\n		async function runCasepack(casepackId, title = casepackId) {\n			if (!sessionId) {\n				setStatus("Session not ready.");\n				return;\n			}\n\n			try {\n				   setStatus(`Starting run for ${title}…`);\n\n				   const started = await startCasepackRun(casepackId, {\n					   sessionId,\n					   contractId: activeContractId,\n					   overrideInputs: null,\n				   });\n\n				   // Poll backend for final run record\n				   const finalRun = await pollRun(started.runId);\n				   window.dispatchEvent(\n					   new CustomEvent("operator:run-update", { detail: finalRun }),\n				   );\n				   setStatus(`Run ${finalRun.runId} finished with status ${finalRun.status}.`);\n			} catch (error) {\n				setStatus(error instanceof Error ? error.message : "Failed to run casepack.");\n			}\n		}\n\n		casepacksList?.addEventListener("click", async (event) => {\n			const target = event.target;\n			if (!(target instanceof HTMLElement)) return;\n\n			const inspectId = target.dataset.inspectId;\n			const inspectTitle = target.dataset.inspectTitle;\n			const runId = target.dataset.runId;\n			const runTitle = target.dataset.runTitle;\n\n			if (inspectId) {\n				await inspectCasepack(inspectId, inspectTitle || inspectId);\n				return;\n			}\n\n			if (runId) {\n				await runCasepack(runId, runTitle || runId);\n			}\n		});\n\n		refreshButton?.addEventListener("click", () => {\n			loadCasepacks();\n		});\n\n		async function bootstrap() {\n			try {\n				const envelope = await ensureOperatorSession({\n					mode: "casepack",\n					activeContractId: "UMA.INTSTACK.v1",\n					activeClosureIds: [],\n					activeCasepackId: null,\n				});\n\n				sessionId = envelope.session.sessionId;\n				activeContractId = envelope.session.activeContractId || "UMA.INTSTACK.v1";\n\n				if (sessionStatus) sessionStatus.textContent = "Session ready.";\n				if (sessionIdEl) sessionIdEl.textContent = sessionId;\n				if (contractEl) contractEl.textContent = activeContractId;\n\n				window.dispatchEvent(\n					new CustomEvent("operator:session-ready", { detail: envelope }),\n				);\n\n				await loadCasepacks();\n			} catch (error) {\n				if (sessionStatus) {\n					sessionStatus.textContent =\n						error instanceof Error ? error.message : "Failed to initialize session.";\n				}\n				setStatus("Could not load casepacks.");\n			}\n		}\n\n		bootstrap();\n	<\/script> '], ["  ", '<section class="casepacks-page" data-astro-cid-jvymcrff> <article class="operator-main-card" data-astro-cid-jvymcrff> <header class="operator-main-card__header" data-astro-cid-jvymcrff> <h2 data-astro-cid-jvymcrff>Casepacks</h2> <button type="button" id="casepacks-refresh" data-astro-cid-jvymcrff>Refresh</button> </header> <p id="casepacks-status" data-astro-cid-jvymcrff>Loading casepacks…</p> <ul class="casepacks-list" id="casepacks-list" data-astro-cid-jvymcrff> <li class="casepacks-list__placeholder" data-astro-cid-jvymcrff>No casepacks found.</li> </ul> </article> <article class="operator-main-card" data-astro-cid-jvymcrff> <h2 data-astro-cid-jvymcrff>Selected Casepack</h2> <pre id="casepacks-detail-json" data-astro-cid-jvymcrff>No casepack selected.</pre> </article> </section>  <script type="module">\n    import { getCorpusObject, searchCorpus } from "../../lib/operator-api.ts";\n    import { ensureOperatorSession } from "../../lib/operator-session.ts";\n\n		const sessionStatus = document.getElementById("casepacks-session-status");\n		const sessionIdEl = document.getElementById("casepacks-session-id");\n		const contractEl = document.getElementById("casepacks-contract-id");\n		const casepacksStatus = document.getElementById("casepacks-status");\n		const casepacksList = document.getElementById("casepacks-list");\n		const detailJson = document.getElementById("casepacks-detail-json");\n		const refreshButton = document.getElementById("casepacks-refresh");\n\n		let sessionId = "";\n		let activeContractId = "UMA.INTSTACK.v1";\n\n		function setStatus(text) {\n			if (casepacksStatus) casepacksStatus.textContent = text;\n		}\n\n		function renderCasepacks(results) {\n			if (!casepacksList) return;\n\n			if (!results.length) {\n				casepacksList.innerHTML = \\`<li class="casepacks-list__placeholder">No casepacks found.</li>\\`;\n				return;\n			}\n\n			casepacksList.innerHTML = results.map((item) => \\`\n				<li class="casepacks-list__item">\n					<div>\n						<strong>\\${item.title}</strong>\n						<p>\\${item.kind}:\\${item.id}</p>\n						\\${item.summary ? \\`<small>\\${item.summary}</small>\\` : ""}\n					</div>\n					<div class="casepacks-list__actions">\n						<button type="button" data-inspect-id="\\${item.id}" data-inspect-title="\\${item.title}">Inspect</button>\n						<button type="button" data-run-id="\\${item.id}" data-run-title="\\${item.title}">Run</button>\n					</div>\n				</li>\n			\\`).join("");\n		}\n\n		async function inspectCasepack(casepackId, title = casepackId) {\n			try {\n				const response = await getCorpusObject("casepack", casepackId);\n\n				if (detailJson) {\n					detailJson.textContent = JSON.stringify(response.object, null, 2);\n				}\n\n				window.dispatchEvent(\n					new CustomEvent("operator:run-update", {\n						detail: {\n							runId: \\`staged:\\${casepackId}\\`,\n							status: "staged",\n							casepackId,\n							contractId: activeContractId,\n							invariants: null,\n							stance: {\n								verdict: "NON_EVALUABLE",\n								rationale: [\n									"Casepack selected and inspected.",\n									"Authoritative run execution endpoint is not wired in this pass."\n								]\n							},\n							artifacts: [],\n						},\n					}),\n				);\n\n				setStatus(\\`Inspected casepack \\${title}.\\`);\n			} catch (error) {\n				if (detailJson) {\n					detailJson.textContent =\n						error instanceof Error ? error.message : "Failed to inspect casepack.";\n				}\n				setStatus("Inspection failed.");\n			}\n		}\n\n		async function loadCasepacks() {\n			setStatus("Loading casepacks…");\n\n			try {\n				const response = await searchCorpus({\n					query: "",\n					kinds: ["casepack"],\n					limit: 50,\n				});\n\n				renderCasepacks(response.results);\n				setStatus(\\`Loaded \\${response.results.length} casepack(s).\\`);\n			} catch (error) {\n				renderCasepacks([]);\n				setStatus(error instanceof Error ? error.message : "Failed to load casepacks.");\n			}\n		}\n\n		async function pollRun(runId) {\n			const maxAttempts = 12;\n			const delayMs = 250;\n\n			for (let attempt = 0; attempt < maxAttempts; attempt += 1) {\n				const run = await getRun(runId);\n\n				window.dispatchEvent(\n					new CustomEvent("operator:run-update", { detail: run }),\n				);\n\n				if (run.status === "completed" || run.status === "failed") {\n					return run;\n				}\n\n				await new Promise((resolve) => setTimeout(resolve, delayMs));\n			}\n\n			return getRun(runId);\n		}\n\n		async function runCasepack(casepackId, title = casepackId) {\n			if (!sessionId) {\n				setStatus("Session not ready.");\n				return;\n			}\n\n			try {\n				   setStatus(\\`Starting run for \\${title}…\\`);\n\n				   const started = await startCasepackRun(casepackId, {\n					   sessionId,\n					   contractId: activeContractId,\n					   overrideInputs: null,\n				   });\n\n				   // Poll backend for final run record\n				   const finalRun = await pollRun(started.runId);\n				   window.dispatchEvent(\n					   new CustomEvent("operator:run-update", { detail: finalRun }),\n				   );\n				   setStatus(\\`Run \\${finalRun.runId} finished with status \\${finalRun.status}.\\`);\n			} catch (error) {\n				setStatus(error instanceof Error ? error.message : "Failed to run casepack.");\n			}\n		}\n\n		casepacksList?.addEventListener("click", async (event) => {\n			const target = event.target;\n			if (!(target instanceof HTMLElement)) return;\n\n			const inspectId = target.dataset.inspectId;\n			const inspectTitle = target.dataset.inspectTitle;\n			const runId = target.dataset.runId;\n			const runTitle = target.dataset.runTitle;\n\n			if (inspectId) {\n				await inspectCasepack(inspectId, inspectTitle || inspectId);\n				return;\n			}\n\n			if (runId) {\n				await runCasepack(runId, runTitle || runId);\n			}\n		});\n\n		refreshButton?.addEventListener("click", () => {\n			loadCasepacks();\n		});\n\n		async function bootstrap() {\n			try {\n				const envelope = await ensureOperatorSession({\n					mode: "casepack",\n					activeContractId: "UMA.INTSTACK.v1",\n					activeClosureIds: [],\n					activeCasepackId: null,\n				});\n\n				sessionId = envelope.session.sessionId;\n				activeContractId = envelope.session.activeContractId || "UMA.INTSTACK.v1";\n\n				if (sessionStatus) sessionStatus.textContent = "Session ready.";\n				if (sessionIdEl) sessionIdEl.textContent = sessionId;\n				if (contractEl) contractEl.textContent = activeContractId;\n\n				window.dispatchEvent(\n					new CustomEvent("operator:session-ready", { detail: envelope }),\n				);\n\n				await loadCasepacks();\n			} catch (error) {\n				if (sessionStatus) {\n					sessionStatus.textContent =\n						error instanceof Error ? error.message : "Failed to initialize session.";\n				}\n				setStatus("Could not load casepacks.");\n			}\n		}\n\n		bootstrap();\n	<\/script> '])), maybeRenderHead()), "left": async ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "left" }, { "default": async ($$result3) => renderTemplate` <section class="operator-sidebar-card" data-astro-cid-jvymcrff> <h2 data-astro-cid-jvymcrff>Session</h2> <p id="casepacks-session-status" data-astro-cid-jvymcrff>Initializing session…</p> <p data-astro-cid-jvymcrff><strong data-astro-cid-jvymcrff>Session ID:</strong> <span id="casepacks-session-id" data-astro-cid-jvymcrff>—</span></p> <p data-astro-cid-jvymcrff><strong data-astro-cid-jvymcrff>Active Contract:</strong> <span id="casepacks-contract-id" data-astro-cid-jvymcrff>—</span></p> </section> ${renderComponent($$result3, "CorpusResolverPanel", $$CorpusResolverPanel, { "data-astro-cid-jvymcrff": true })} ` })}`, "right": async ($$result2) => renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "slot": "right" }, { "default": async ($$result3) => renderTemplate` ${renderComponent($$result3, "RunSummaryCard", $$RunSummaryCard, { "data-astro-cid-jvymcrff": true })} ` })}` })}`;
}, "/home/runner/work/GENERATIVE-COLLAPSE-DYNAMICS/GENERATIVE-COLLAPSE-DYNAMICS/web/src/pages/operator/casepacks.astro", void 0);

const $$file = "/home/runner/work/GENERATIVE-COLLAPSE-DYNAMICS/GENERATIVE-COLLAPSE-DYNAMICS/web/src/pages/operator/casepacks.astro";
const $$url = "/GENERATIVE-COLLAPSE-DYNAMICS/operator/casepacks";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Casepacks,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
